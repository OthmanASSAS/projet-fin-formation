/* const express = require('express');
const router = express.Router();
let connection = require('../db_connect.js');
 */
//console.log(connection);

// class Message {
  
//     static create (content, callBack) {
// connection.query('INSERT INTO pre-ins SET content = ?, date_creation = ?', [content, new Date()], (err, result) => {
//     if (err) throw err;
// callBack(result)
// })
//     }
// };

// module.exports = usagers;
/* router.get('/',(req, res, next)=>{

    connection.query("SELECT * FROM `user`", function (err, results) {
        if (err) console.error('Mysql request error: "', err);
        console.log("RESULT: ", results);

        if (results.length > 0) {
            var firstResult = results[0];
            console.log('id: ' + firstResult['id']);
            console.log('Nom: ' + firstResult['nom']);
            console.log('Prénom: ' + firstResult['prenom']);
        } else {
            console.log("Pas de données");
        }
    })

}) */



//connection.end();

/* module.exports = router; */
